services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: file-ingestion-api:latest
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./:/app
      - uploads-data:/app/uploads
    restart: unless-stopped

  postgres:
    image: postgres:16
    env_file:
      - .env
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d file_ingestion"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  redis:
    image: redis:7
    env_file:
      - .env
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  ftp:
    image: delfer/alpine-ftp-server
    container_name: file-ingestion-ftp
    ports:
      - "21:21"
      - "21000-21010:21000-21010" # Passive ports for data transfer
    env_file:
      - .env
    volumes:
      # Both containers now share the exact same virtual drive
      - uploads-data:/ftp
      # - D:\Zenteiq\Tasks\uploads:/ftp
    restart: unless-stopped

  ext-ftp:
    image: delfer/alpine-ftp-server
    container_name: file-ingestion-ext-ftp
    ports:
      - "2121:21"
      - "21100-21110:21100-21110" # Passive ports for external pull data transfer
    env_file:
      - .env
    environment:
      - USERS=${FTP_SERVER_USER}|${FTP_SERVER_PASSWORD}|/ftp|1000
      - ADDRESS=${ADDRESS}
      - MIN_PORT=21100
      - MAX_PORT=21110
    volumes:
      - ext-ftp-data:/ftp
    restart: unless-stopped

volumes:
  postgres-data:
  redis-data:
  uploads-data:
  ext-ftp-data: